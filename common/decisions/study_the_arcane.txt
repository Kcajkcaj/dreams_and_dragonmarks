study_the_arcane = {
	picture = "gfx/interface/illustrations/decisions/decision_golden_age.dds"
	
	cost = {
		gold = 100
	}
	
	is_shown = {
		is_landed = yes
		has_trait = mage
	}
	
	cooldown = {
		years = 5
	}
	
	widget = {
		gui = "decision_view_widget_study_the_arcane"
		controller = decision_option_list_controller
		decision_has_second_step = yes
		decision_custom_widget_container = "custom_widgets_container_step_two"
		decision_to_second_step_button = "study_the_arcane_decision_next_step_option"
		
		item = {
			value = abjuration
			is_shown = {has_trait_xp = {trait = mage track = abjuration value < 100}}
			localization = study_magic_decision_option_abjuration
			icon = "gfx/interface/icons/trait_level_tracks/abjuration.dds"
			add_trait_xp = {trait = mage track = abjuration value = 10}
			ai_chance = {
				value = 10
				if = {
					limit = {has_trait_xp = {trait = mage track = abjuration value > 0}}
					add = 100
				}
			}
		}
		item = {
			value = conjuration
			is_shown = {has_trait_xp = {trait = mage track = conjuration value < 100}}
			localization = study_magic_decision_option_conjuration
			icon = "gfx/interface/icons/trait_level_tracks/conjuration.dds"
			add_trait_xp = {trait = mage track = conjuration value = 10}
			ai_chance = {
				value = 10
				if = {
					limit = {has_trait_xp = {trait = mage track = conjuration value > 0}}
					add = 100
				}
			}
		}
		item = {
			value = divination
			is_shown = {has_trait_xp = {trait = mage track = divination value < 100}}
			localization = study_magic_decision_option_divination
			icon = "gfx/interface/icons/trait_level_tracks/divination.dds"
			add_trait_xp = {trait = mage track = divination value = 10}
			ai_chance = {
				value = 10
				if = {
					limit = {has_trait_xp = {trait = mage track = divination value > 0}}
					add = 100
				}
			}
		}
		item = {
			value = enchantment
			is_shown = {has_trait_xp = {trait = mage track = enchantment value < 100}}
			localization = study_magic_decision_option_enchantment
			icon = "gfx/interface/icons/trait_level_tracks/enchantment.dds"
			add_trait_xp = {trait = mage track = enchantment value = 10}
			ai_chance = {
				value = 10
				if = {
					limit = {has_trait_xp = {trait = mage track = enchantment value > 0}}
					add = 100
				}
			}
		}
		item = {
			value = evocation
			is_shown = {has_trait_xp = {trait = mage track = evocation value < 100}}
			localization = study_magic_decision_option_evocation
			icon = "gfx/interface/icons/trait_level_tracks/evocation.dds"
			add_trait_xp = {trait = mage track = evocation value = 10}
			ai_chance = {
				value = 10
				if = {
					limit = {has_trait_xp = {trait = mage track = evocation value > 0}}
					add = 100
				}
			}
		}
		item = {
			value = illusion
			is_shown = {has_trait_xp = {trait = mage track = illusion value < 100}}
			localization = study_magic_decision_option_illusion
			icon = "gfx/interface/icons/trait_level_tracks/illusion.dds"
			add_trait_xp = {trait = mage track = illusion value = 10}
			ai_chance = {
				value = 10
				if = {
					limit = {has_trait_xp = {trait = mage track = illusion value > 0}}
					add = 100
				}
			}
		}
		item = {
			value = necromancy
			is_shown = {has_trait_xp = {trait = mage track = necromancy value < 100}}
			localization = study_magic_decision_option_necromancy
			icon = "gfx/interface/icons/trait_level_tracks/necromancy.dds"
			add_trait_xp = {trait = mage track = necromancy value = 10}
			ai_chance = {
				value = 10
				if = {
					limit = {has_trait_xp = {trait = mage track = necromancy value > 0}}
					add = 100
				}
			}
		}
		item = {
			value = transmutation
			is_shown = {has_trait_xp = {trait = mage track = transmutation value < 100}}
			localization = study_magic_decision_option_transmutation
			icon = "gfx/interface/icons/trait_level_tracks/transmutation.dds"
			add_trait_xp = {trait = mage track = transmutation value = 10}
			ai_chance = {
				value = 10
				if = {
					limit = {has_trait_xp = {trait = mage track = transmutation value > 0}}
					add = 100
				}
			}
		}
	}
	
	effect = {
		add_stress = 50
		if = {
			limit = {scope:abjuration = yes}
			add_trait_xp = {trait = mage track = abjuration value = 10}
		}
		if = {
			limit = {scope:conjuration = yes}
			add_trait_xp = {trait = mage track = conjuration value = 10}
		}
		if = {
			limit = {scope:divination = yes}
			add_trait_xp = {trait = mage track = divination value = 10}
		}
		if = {
			limit = {scope:enchantment = yes}
			add_trait_xp = {trait = mage track = enchantment value = 10}
		}
		if = {
			limit = {scope:evocation = yes}
			add_trait_xp = {trait = mage track = evocation value = 10}
		}
		if = {
			limit = {scope:illusion = yes}
			add_trait_xp = {trait = mage track = illusion value = 10}
		}
		if = {
			limit = {scope:necromancy = yes}
			add_trait_xp = {trait = mage track = necromancy value = 10}
		}
		if = {
			limit = {scope:transmutation = yes}
			add_trait_xp = {trait = mage track = transmutation value = 10}
		}
	}
}